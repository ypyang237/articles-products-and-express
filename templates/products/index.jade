h1(id='productHeader') Products

block content
  div(id="container")

    div
      each product in products
        h1= 'name : '+ product.name
        h2= 'price : '+ product.price
        h3= 'inventory : '+ product.inventory